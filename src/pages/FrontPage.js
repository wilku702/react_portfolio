import React from 'react';
import Header from '../components/Header/Header';

const FrontPage = ({}) => {
  return (
    <div className="page">
      <Header />
    </div>
  );
};

export default FrontPage;
